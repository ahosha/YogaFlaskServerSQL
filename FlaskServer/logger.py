## This Python file uses the following encoding: utf-8
#import logging
#import logging.handlers

#LOG_FILE_SIZE = 250000
##LOG_FILE_NAME =?'loggerout.out' 

## define the log line format

#log_formatter = logging.Formatter('%(levelname)s %(funcName)s(%(lineno)d) %(message)s')

## Set up a specific logger with our desired output level

#Logger = logging.getLogger('FlaskLogger')

#Logger.setLevel(logging.INFO)

## Add the log message handler to the logger
#handler = logging.handlers.RotatingFileHandler('loggerout.out', maxBytes=LOG_FILE_SIZE, backupCount=1)

## set log format
#handler.setFormatter(log_formatter)

#Logger.addHandler(handler) 
